import React from 'react'

const SearchBar = () => {
  return (
    <form>
      <input type="text" value={} onChange={} placeholder="Search a book by title" />
      <input type="submit" value="Search" />
    </form>
  )
}

export default SearchBar
